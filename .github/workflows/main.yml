name: 🚀 Deploy website on push
on:
  push
    branches: [development]
    work_dispatch:
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@development

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.3
      with:
        server: ${{ secrets.FTP_SERVER_CDA }}
        username: ${{ secrets.FTP_USERNAME_CDA }}
        password: ${{ secrets.FTP_PASSWORD_CDA }}
        dry-run: true
